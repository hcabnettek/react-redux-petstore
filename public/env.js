(function (window) {
  window.__env = window.__env || {};

  window.__env.features = {
    storage: "enabled",
    timeToLive: "enabled",
    testing: "enabled",
    multiCloud: "enabled",
  };
})(this);
